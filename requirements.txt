# PyTorch（需先单独安装，见 README.md）
# GPU 版（当前使用，2.5.1+cu121，需 CUDA 12.1）：
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# CPU 版（仅 CPU，无需显卡驱动）：
#   pip install torch
torch>=2.1.0
torch-geometric>=2.4.0
numpy>=1.26.0
matplotlib>=3.8.0
networkx>=3.0
